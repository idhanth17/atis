# Copy to .env (gitignored). NON-SECRET config only.
# Real secrets (Kite api_secret, access tokens — Phase 4) go in the OS keyring,
# never in this file. See docs/SECURITY.md §2.
ATIS_MODE=paper
ATIS_DB_PATH=data/atis.db
ATIS_DATA_DIR=data
ATIS_KILL_FILE=KILL
